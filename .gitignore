.DS_Store
.cache
.claude

ref/
/assets/
target/
voices/
out/
dist/

# Dev scratch examples — kept on disk for local runs, never tracked.
examples/
# One-off dev tools (q8 converter, whisper-WER, gen) live here as standalone
# path-dep crates — usable but never part of the tracked codebase / workspace.
artifacts/

*.wav